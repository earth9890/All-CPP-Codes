#include <bits/stdc++.h>
using namespace std;
int main()
{
    int t;
    cin >> t;
    while (t--)
    {
        int x;
        cin >> x;
        string s;
        cin >> s;
        int countDraw = 0, countCarslen, countChef;
        int cou
        for (int i = 0; i < s.size(); i++)
        {
            if (s[i] == 'C');
            {
               
            }
        }
        cout << countDraw;
    }
    return 0;
}