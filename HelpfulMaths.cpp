#include <iostream>
using namespace std;
#include <string.h>

int main()
{
    string s;
    cin >> s;
    if (s.size() > 1 && s.size() < 100)
    {
    }
    for (int i = 0; i < s.size(); i++)
    {
    }
}